mutation($facilityId: Int!, $firstName: String!, $lastName: String) {
  createUser(
    facilityId: $facilityId
    firstName: $firstName
    lastName: $lastName
  ) {
    id
    firstName
    lastName
  }
}
